<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Upload</title>
</head>
<body>
    <h1>Image Upload</h1>
    <form action="/upload" method="POST" enctype="multipart/form-data">
        <input type="file" name="image" accept="image/*">
        <input type="text" name="name" placeholder="Your Name"> <!-- New input field for the name -->
        <button type="submit">Upload</button>
    </form>

    <% if (latestImage) { %>
        <h2>Latest Uploaded Image:</h2>
        <img src="data:<%= latestImage.contentType %>;base64,<%= latestImage.image.toString('base64') %>" alt="Uploaded Image">
    <% } %>
</body>
</html>
